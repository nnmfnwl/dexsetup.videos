Would we like to auto-configure tor-browser default profile. If we previous step installed tor-browser it is recommended to configure or reconfigure default profile.

Now, as all selected components been installed and configured and dex-setup installer has successfully finished, before we return back to dex-setup installer page and continue according to instructions, at first lets take a look at newly created dex-setup directory structure and components included.
